# 快速开始指南

本项目是一个基于React Native的WebView应用，已完成所有功能开发和CI/CD配置。

## 项目已完成功能 ✅

1. ✅ **Loading过渡页面**：应用启动时显示loading图片，1秒后自动跳转到主页
2. ✅ **WebView主页**：全屏WebView加载网页（默认：www.baidu.com）
3. ✅ **双平台支持**：同时支持Android和iOS
4. ✅ **GitHub Actions CI/CD**：自动打包Android APK/AAB和iOS IPA
5. ✅ **详细文档**：完整的Android和iOS打包说明

## 项目结构

```
/workspace/
├── src/
│   ├── screens/
│   │   ├── LoadingScreen.tsx    # Loading页面（1秒过渡）
│   │   └── HomeScreen.tsx       # WebView主页
│   ├── navigation/
│   │   ├── AppNavigator.tsx     # 导航配置
│   │   └── types.ts             # TypeScript类型定义
├── assets/
│   └── loading.png              # Loading页面展示的图片
├── android/                      # Android原生代码
├── ios/                         # iOS原生代码
├── .github/workflows/
│   └── build-release.yml        # CI/CD配置
├── android打包说明.md            # Android详细打包教程
├── ios打包说明.md               # iOS详细打包教程
└── README.md                    # 完整项目说明
```

## 立即开始使用

### 1. 安装依赖

```bash
npm install
```

### 2. 运行应用

**Android:**
```bash
npm run android
```

**iOS (仅Mac):**
```bash
cd ios && pod install && cd ..
npm run ios
```

### 3. 修改WebView URL

编辑 `src/screens/HomeScreen.tsx`，修改第15行：

```typescript
<WebView
  source={{ uri: 'https://www.baidu.com' }}  // 改成你想要的URL
  // ...
/>
```

### 4. 替换Loading图片

将你的图片放到 `assets/loading.png`（建议尺寸：400x300或更大）

## 自动化打包配置

### 配置GitHub Secrets

在GitHub仓库 Settings > Secrets and variables > Actions 中添加：

#### Android Secrets:
- `ANDROID_KEYSTORE_BASE64` - 签名密钥的Base64编码
- `ANDROID_KEY_ALIAS` - 密钥别名
- `ANDROID_STORE_PASSWORD` - 密钥库密码
- `ANDROID_KEY_PASSWORD` - 密钥密码

#### iOS Secrets:
- `IOS_CERTIFICATE_BASE64` - P12证书的Base64编码
- `IOS_CERTIFICATE_PASSWORD` - P12密码
- `IOS_PROVISIONING_PROFILE_BASE64` - Provisioning Profile的Base64编码
- `IOS_PROVISIONING_PROFILE_NAME` - Profile名称
- `IOS_CODE_SIGN_IDENTITY` - 签名身份
- `IOS_TEAM_ID` - Team ID
- `IOS_KEYCHAIN_PASSWORD` - Keychain密码（任意设置）

**详细配置步骤请查看：**
- [android打包说明.md](./android打包说明.md)
- [ios打包说明.md](./ios打包说明.md)

### 触发自动打包

#### 方式1：推送代码
```bash
git add .
git commit -m "Your commit message"
git push origin main
```

#### 方式2：创建版本标签（会创建GitHub Release）
```bash
git tag v1.0.0
git push origin v1.0.0
```

打包完成后，可以在GitHub Actions页面下载：
- `android-apk` - Android安装包
- `android-aab` - Google Play上架包
- `ios-ipa` - iOS安装包

## 本地打包

### Android

```bash
# 生成APK（直接安装）
cd android && ./gradlew assembleRelease

# 生成AAB（Google Play上架）
cd android && ./gradlew bundleRelease
```

生成的文件：
- APK: `android/app/build/outputs/apk/release/app-release.apk`
- AAB: `android/app/build/outputs/bundle/release/app-release.aab`

### iOS

```bash
cd ios

# 安装依赖
pod install

# 构建Archive
xcodebuild -workspace MyWebViewApp.xcworkspace \
  -scheme MyWebViewApp \
  -configuration Release \
  -archivePath ./build/MyWebViewApp.xcarchive \
  archive

# 导出IPA
xcodebuild -exportArchive \
  -archivePath ./build/MyWebViewApp.xcarchive \
  -exportOptionsPlist ExportOptions.plist \
  -exportPath ./build
```

## 应用商店上架

### Google Play（Android）

1. 注册Google Play开发者账号（$25一次性费用）
2. 创建应用并填写商店信息
3. 上传AAB文件（`app-release.aab`）
4. 提交审核

**详细步骤：** [android打包说明.md](./android打包说明.md) 第5节

### App Store（iOS）

1. 注册Apple Developer账号（$99/年）
2. 配置App Store Connect
3. 上传IPA文件
4. 提交审核

**详细步骤：** [ios打包说明.md](./ios打包说明.md) 第6-8节

## 常用修改

### 修改应用名称

- **Android**: `android/app/src/main/res/values/strings.xml`
- **iOS**: Xcode中的Display Name

### 修改应用图标

- **Android**: 替换 `android/app/src/main/res/mipmap-*/ic_launcher.png`
- **iOS**: Xcode中的AppIcon

### 修改Bundle ID

- **Android**: `android/app/build.gradle` 的 `applicationId`
- **iOS**: Xcode中的Bundle Identifier

### 修改版本号

- **Android**: `android/app/build.gradle` 的 `versionCode` 和 `versionName`
- **iOS**: Xcode中的Version和Build

## 技术栈

- React Native 0.82.1
- React 19.1.1
- React Navigation（导航）
- React Native WebView（网页展示）
- TypeScript（类型安全）
- GitHub Actions（CI/CD）

## 获取帮助

- **完整文档**: [README.md](./README.md)
- **Android打包**: [android打包说明.md](./android打包说明.md)
- **iOS打包**: [ios打包说明.md](./ios打包说明.md)
- **React Native官方**: https://reactnative.dev/

## 项目特点

✨ **开箱即用** - 所有代码和配置已完成，可直接运行
🚀 **自动化CI/CD** - 推送代码即可自动打包
📱 **双平台支持** - 同时支持Android和iOS
📝 **完整文档** - 详细的打包和上架说明
🔒 **安全配置** - 所有敏感信息使用GitHub Secrets管理

## 下一步

1. ✅ 安装依赖并运行项目
2. ✅ 修改WebView URL为你的网页
3. ✅ 替换loading图片和应用图标
4. ✅ 配置GitHub Secrets
5. ✅ 推送代码触发自动打包
6. ✅ 上架到应用商店

祝你的应用上架成功！🎉
